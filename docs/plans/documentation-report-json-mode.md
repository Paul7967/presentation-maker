# Отчёт документации: режим JSON (Этап 7)

**Дата:** 2025-03-08  
**Связь:** [dev_plan_2.md](dev_plan_2.md)

## Выполнено

1. **README.md** — обновлён раздел «Использование»:
   - Описаны два режима ввода: «По слайдам» и «Через JSON».
   - Для режима «По слайдам»: пошаговые действия, включая загрузку из .json при необходимости.
   - Для режима «Через JSON»: переключение, редактирование JSON, загрузка из файла, кнопка «Сохранить в JSON», генерация.
   - Указано поведение при переключении обратно в «По слайдам» (валидация, сообщение об ошибке при невалидном JSON).
   - Добавлено ограничение: файл .json при загрузке — до 2 MB.

2. **docs/architecture.md** — раздел 3.1 (Фронтенд) уже содержит абзац «Два режима ввода (режим JSON)» с ссылкой на [architecture-json-mode.md](architecture-json-mode.md). Дополнений не потребовалось.

## Итог

Пользовательская документация (README) отражает новый функционал; архитектурный документ уже описывает два режима ввода на фронтенде.

Выполнено агентом: documentation
